# ===========================================
# API BACKEND
# ===========================================
# URL da API do backend (sem /api no final)
# Better Auth espera a URL base, e adiciona /api/auth automaticamente
NEXT_PUBLIC_API_URL=http://localhost:4000
